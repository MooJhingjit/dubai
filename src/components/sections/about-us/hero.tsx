import React from "react";

const Hero = () => {
  return (
    <div
      className="relative w-full h-[376px] bg-cover bg-top bg-no-repeat"
      style={{
        backgroundImage: "url(/dubai/about/hero.png)"
      }}
    />
  );
};

export default Hero;
